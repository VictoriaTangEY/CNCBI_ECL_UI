1. 打开mobax之后需要进行的操作：
sudo su -
cd /var/www/mywebsite/flaskapp
source /home/svreclcalusr/venv_eclengine/bin/activate 

2. Flask部分：
# 后端python文件：/var/www/mywebsite/flaskapp/app.py


3. Gunicorn部分：
# Gunicorn service: /etc/systemd/system/flaskapp.service


4. Nginx部分：
# Ngnix proxy: /etc/nginx/conf.d/mywebsite.conf



98. 权限：
ls -ld /path
sudo chmod -R 755 /path
ls -ld /path
sudo systemctl restart nginx


99. 防火墙：
sudo firewall-cmd --list-all
# 添加http：
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
# 添加端口：
sudo firewall-cmd --add-port=5010/tcp --permanent
sudo firewall-cmd --list-ports


100. 重启尝试
# 重启flaskapp：
sudo systemctl daemon-reload
sudo systemctl start flaskapp
sudo systemctl enable flaskapp

# 重启nginx：
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl restart flaskapp

# 结束端口进程：
sudo lsof -i :5010
sudo kill <PID>
pkill gunicorn
